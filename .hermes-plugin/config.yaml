# Mainframe Hermes plugin wiring — generated by tooling/generate.ts; do not edit by hand.
# Merge into ~/.hermes/config.yaml. See README.md for the share-video skill setup.
mcp_servers:
  mainframe:
    url: https://mcp.mainframe.app/mcp
hooks:
  pre_llm_call:
    - command: mainframe-hook-hermes
      timeout: 30
